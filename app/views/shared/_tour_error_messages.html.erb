<% if @tour.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      The form contains <%= pluralize(@tour.errors.count, "error") %>:
    
    <ul>
    <% @tour.errors.full_messages.each do |msg| %>
      <li>• <%= msg %></li>
    <% end %>
    </ul>
    </div>
  </div>
<% end %>