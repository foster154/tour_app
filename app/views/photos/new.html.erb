<h1>Add Photos</h1>

<h3>Let's make this thing pretty! Add some spectacular pics.</h3>

<%= form_for(@photo, :tour_id => @tour.id) do |f| %>
	<%= render(:partial => "form", :locals => {:f => f}) %>
<% end %>

<%= link_to 'Back', photos_path %>
