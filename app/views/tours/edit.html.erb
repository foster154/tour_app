<% provide(:title, 'Edit Tour') %>
<h1>Edit a Tour</h1>

<div class="row">
	<div class="col-md-4 col-md-offset-4">
		<%= form_for(@tour) do |f| %>
			<%= render 'form', f: f %>
			<%= f.submit "Update Tour", class: "btn btn-large btn-primary" %>
		<% end %>
	</div>
</div>